import { IncomeAnalysisResult } from "../../types/incomeAnalysis";
import { formatCurrencyBR } from "../../lib/income-analysis/formatters";
export function PayerRecurrenceTable({ result }: { result: IncomeAnalysisResult }) { return <section className="rounded-lg border bg-white p-5 shadow-sm sm:p-6"><h2 className="text-xl font-bold">Pagadores Recorrentes</h2><div className="mt-4 overflow-x-auto"><table className="min-w-[650px] w-full text-sm"><thead className="bg-slate-100 text-left text-xs"><tr><th className="p-3">Pagador/origem</th><th className="p-3">Meses</th><th className="p-3">Ocorrências</th><th className="p-3">Total recebido</th><th className="p-3">Média mensal</th></tr></thead><tbody>{result.recurrence.map((payer) => <tr key={payer.payer} className="border-t"><td className="p-3 font-semibold">{payer.payer}</td><td className="p-3">{payer.months}</td><td className="p-3">{payer.occurrences}</td><td className="p-3">{formatCurrencyBR(payer.total)}</td><td className="p-3">{formatCurrencyBR(payer.average)}</td></tr>)}</tbody></table>{!result.recurrence.length && <p className="py-8 text-center text-sm text-slate-500">Nenhum pagador em movimentações consideradas.</p>}</div></section>; }
