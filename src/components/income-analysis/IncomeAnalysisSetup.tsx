import { IncomeAnalysisForm } from "../../types/incomeAnalysis";
import { INCOME_PROFILE_LABELS } from "../../lib/income-analysis/incomeRules";
import { listCompetences } from "../../lib/income-analysis/incomeCalculator";

const cls = "mt-2 h-12 w-full rounded-lg border border-slate-300 bg-white px-3 text-sm font-medium outline-none focus:border-goodgreen-500 focus:ring-4 focus:ring-goodgreen-500/10";
const banks = ["Caixa", "Banco do Brasil", "Bradesco", "Itaú", "Santander", "Inter", "Nubank", "C6", "Outro"];
export function IncomeAnalysisSetup({ form, onChange }: { form: IncomeAnalysisForm; onChange: (form: IncomeAnalysisForm) => void }) {
  const months = listCompetences(form.periodStart, form.periodEnd).length;
  return <section className="rounded-lg border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><h2 className="text-xl font-bold">Dados da Apuração</h2><p className="mt-1 text-sm text-slate-500">Defina o período completo antes de lançar ou importar as movimentações.</p><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><label className="text-sm font-semibold xl:col-span-2">Nome do cliente ou processo *<input className={cls} value={form.clientName} onChange={(e) => onChange({ ...form, clientName: e.target.value })} /></label><label className="text-sm font-semibold">Perfil de renda *<select className={cls} value={form.incomeProfile} onChange={(e) => onChange({ ...form, incomeProfile: e.target.value as IncomeAnalysisForm["incomeProfile"] })}><option value="">Selecione</option>{Object.entries(INCOME_PROFILE_LABELS).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label className="text-sm font-semibold">Participantes compondo renda<select className={cls} value={form.participantCount} onChange={(e) => onChange({ ...form, participantCount: e.target.value as IncomeAnalysisForm["participantCount"] })}><option value="1">1</option><option value="2">2</option><option value="3_PLUS">3 ou mais</option></select></label><label className="text-sm font-semibold">Banco analisado<select className={cls} value={form.bank} onChange={(e) => onChange({ ...form, bank: e.target.value })}>{banks.map((bank) => <option key={bank}>{bank}</option>)}</select></label><label className="text-sm font-semibold">Quantidade de contas<input type="text" inputMode="numeric" className={cls} value={form.accountCount} onChange={(e) => onChange({ ...form, accountCount: Math.max(1, Number(e.target.value.replace(/\D/g, "")) || 1) })} /></label><label className="text-sm font-semibold">Período inicial *<input type="month" className={cls} value={form.periodStart} onChange={(e) => onChange({ ...form, periodStart: e.target.value })} /></label><label className="text-sm font-semibold">Período final *<input type="month" className={cls} value={form.periodEnd} onChange={(e) => onChange({ ...form, periodEnd: e.target.value })} /></label><div className="rounded-lg border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-semibold text-slate-500">Meses calculados</p><p className="mt-2 text-2xl font-bold text-slate-950">{months}</p></div><label className="text-sm font-semibold xl:col-span-2">Tipo de análise<select className={cls} value={form.analysisType} onChange={(e) => onChange({ ...form, analysisType: e.target.value as IncomeAnalysisForm["analysisType"] })}><option value="SINGLE_ACCOUNT">Uma única conta</option><option value="MULTIPLE_SAME_OWNER">Múltiplas contas do mesmo titular</option><option value="MULTIPLE_PARTICIPANTS">Contas de participantes diferentes</option></select></label><label className="text-sm font-semibold xl:col-span-2">Atividade profissional declarada<input className={cls} value={form.declaredActivity} onChange={(e) => onChange({ ...form, declaredActivity: e.target.value })} /></label><label className="text-sm font-semibold md:col-span-2 xl:col-span-4">Observações gerais<textarea className="mt-2 min-h-24 w-full rounded-lg border border-slate-300 bg-white p-3 text-sm outline-none focus:border-goodgreen-500 focus:ring-4 focus:ring-goodgreen-500/10" value={form.analystNotes} onChange={(e) => onChange({ ...form, analystNotes: e.target.value })} /></label></div></section>;
}
