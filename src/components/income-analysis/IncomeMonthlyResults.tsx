import { formatCompetence, formatCurrencyBR } from "../../lib/income-analysis/formatters";
import { getMonthStatusLabel } from "../../lib/statement-analysis/presentationLabels";
import type { AutomatedIncomeResult } from "../../types/statementAnalysis";

export function IncomeMonthlyResults({ result }: { result: AutomatedIncomeResult }) {
  return <section className="rounded-lg border border-slate-200 bg-white shadow-sm"><div className="p-5"><h2 className="text-lg font-bold">Resultado por mês</h2><p className="mt-1 text-sm text-slate-500">A média principal usa apenas competências completas.</p></div><div className="overflow-x-auto border-t"><table className="min-w-[1050px] w-full text-sm"><thead className="bg-slate-50 text-left text-xs text-slate-500"><tr>{["Competência", "Créditos", "Débitos", "Renda confirmada", "Renda potencial", "Excluídos", "Pendentes", "Pagadores", "Situação"].map((label) => <th key={label} className="px-4 py-3">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{result.months.map((month) => <tr key={month.competence}><td className="px-4 py-3 font-bold">{formatCompetence(month.competence)}</td><td className="px-4 py-3">{formatCurrencyBR(month.totalCredits)}</td><td className="px-4 py-3">{formatCurrencyBR(month.totalDebits)}</td><td className="px-4 py-3 font-bold text-emerald-700">{formatCurrencyBR(month.confirmedIncome)}</td><td className="px-4 py-3">{formatCurrencyBR(month.potentialIncome)}</td><td className="px-4 py-3">{formatCurrencyBR(month.excludedAmount)}</td><td className="px-4 py-3 text-amber-700">{formatCurrencyBR(month.pendingAmount)}</td><td className="px-4 py-3">{month.payerCount}</td><td className="px-4 py-3"><span className={`rounded-full px-2 py-1 text-xs font-bold ${month.complete ? "bg-emerald-100 text-emerald-700" : "bg-amber-100 text-amber-800"}`}>{getMonthStatusLabel(month.complete ? "COMPLETE" : "INCOMPLETE")}</span></td></tr>)}</tbody></table></div></section>;
}
