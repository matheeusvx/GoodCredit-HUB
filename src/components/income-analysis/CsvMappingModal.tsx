import { CsvColumnMapping, CsvPreview } from "../../types/incomeAnalysis";

export function CsvMappingModal({ preview, mapping, onMapping, onConfirm, onClose }: { preview: CsvPreview; mapping: CsvColumnMapping; onMapping: (mapping: CsvColumnMapping) => void; onConfirm: () => void; onClose: () => void }) {
  const fields: Array<[keyof CsvColumnMapping, string, boolean]> = [["date", "Data", true], ["description", "Descrição", true], ["amount", "Valor", true], ["type", "Crédito/débito", false], ["payer", "Pagador", false], ["account", "Conta", false], ["competence", "Competência", false]];
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><div className="max-h-[90vh] w-full max-w-4xl overflow-auto rounded-lg bg-white p-6 shadow-xl"><h2 className="text-xl font-bold">Mapear colunas do CSV</h2><p className="mt-1 text-sm text-slate-500">{preview.rows.length} linhas encontradas. Revise a prévia antes de importar.</p><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{fields.map(([key, label, required]) => <label key={key} className="text-sm font-semibold">{label}{required ? " *" : ""}<select className="mt-2 h-11 w-full rounded-lg border border-slate-300 px-3" value={mapping[key]} onChange={(e) => onMapping({ ...mapping, [key]: e.target.value })}><option value="">Não mapear</option>{preview.headers.map((header) => <option key={header}>{header}</option>)}</select></label>)}</div><div className="mt-5 overflow-x-auto rounded-lg border"><table className="min-w-[700px] w-full text-xs"><thead className="bg-slate-100"><tr>{preview.headers.map((header) => <th key={header} className="p-2 text-left">{header}</th>)}</tr></thead><tbody>{preview.rows.slice(0, 5).map((row, index) => <tr key={index} className="border-t">{row.map((cell, cellIndex) => <td key={cellIndex} className="max-w-48 truncate p-2">{cell}</td>)}</tr>)}</tbody></table></div><div className="mt-5 flex justify-end gap-3"><button className="btn-muted" type="button" onClick={onClose}>Cancelar</button><button className="btn-primary" type="button" disabled={!mapping.date || !mapping.description || !mapping.amount} onClick={onConfirm}>Confirmar importação</button></div></div></div>;
}
