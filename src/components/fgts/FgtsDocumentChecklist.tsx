import { CheckCircle2 } from "lucide-react";
import { FGTS_DOCUMENTS } from "../../lib/fgts/fgtsRules";

export function FgtsDocumentChecklist({ checked, onChange }: { checked: Record<string, boolean>; onChange: (checked: Record<string, boolean>) => void }) {
  return <section className="rounded-lg border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-start gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-lg bg-goodgreen-50 text-goodgreen-700"><CheckCircle2 className="h-5 w-5" /></div><div><h2 className="text-xl font-bold">Documentos e Informações para Uso do FGTS</h2><p className="mt-1 text-sm text-slate-500">Marque os itens já recebidos. O progresso fica salvo somente neste navegador.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{FGTS_DOCUMENTS.map((item) => <label key={item.id} className={`flex items-start gap-3 rounded-lg border p-4 text-sm ${checked[item.id] ? "border-emerald-200 bg-emerald-50" : "border-slate-200 bg-white"}`}><input type="checkbox" className="mt-0.5 h-4 w-4 accent-goodgreen-600" checked={Boolean(checked[item.id])} onChange={(e) => onChange({ ...checked, [item.id]: e.target.checked })} /><span><strong className="font-semibold text-slate-900">{item.label}</strong>{item.required && <span className="ml-2 rounded-full bg-rose-100 px-2 py-0.5 text-[10px] font-bold text-rose-700">Obrigatório</span>}</span></label>)}</div><div className="mt-5 space-y-2 rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900"><p><strong>Regra operacional GoodCredit:</strong> o extrato atualizado do FGTS é obrigatório junto aos demais documentos.</p><p>O número do PIS/PASEP/NIS também deverá ser informado ou comprovado.</p></div></section>;
}
