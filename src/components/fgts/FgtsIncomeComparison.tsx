import { formatCurrencyBR, formatPercentBR } from "../../lib/financial";
import { FgtsIncomeForm, FgtsIncomeResult } from "../../types/fgts";

export function FgtsIncomeComparison({ form, result }: { form: FgtsIncomeForm; result: FgtsIncomeResult }) {
  const facts = [["Holerite", formatCurrencyBR(form.payslipGross)], ["Depósito FGTS", formatCurrencyBR(form.monthlyDeposit)], ["Percentual", formatPercentBR(form.contributionRate)], ["Renda estimada", formatCurrencyBR(result.estimatedIncome)], ["Diferença", formatCurrencyBR(result.differenceAmount)], ["Diferença percentual", formatPercentBR(result.differencePercent)], ["Maior valor", formatCurrencyBR(result.highestIncome)], ["Valor considerado", formatCurrencyBR(result.consideredIncome)]];
  return <div className="mt-5"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{facts.map(([label, value]) => <div key={label} className="rounded-lg border border-slate-200 bg-slate-50 p-4"><p className="text-xs font-semibold text-slate-500">{label}</p><p className="mt-1 font-bold text-slate-950">{value}</p></div>)}</div><div className="mt-4 flex flex-wrap items-center gap-3"><span className={`rounded-full px-3 py-1 text-xs font-bold ${result.status === "REVIEW_REQUIRED" ? "bg-amber-100 text-amber-800" : result.status === "COHERENT" ? "bg-emerald-100 text-emerald-800" : "bg-blue-100 text-blue-800"}`}>{result.statusLabel}</span><span className="text-sm text-slate-600">Critério: {result.criterionLabel}</span></div>{result.alerts.map((alert) => <p key={alert} className="mt-3 rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800">{alert}</p>)}</div>;
}
