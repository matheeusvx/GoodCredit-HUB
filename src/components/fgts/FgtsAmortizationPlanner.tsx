import { Plus } from "lucide-react";
import { formatCurrencyBR, parseNumberBR } from "../../lib/financial";
import { FgtsAmortizationEvent, FgtsProjectionForm, FgtsProjectionResult } from "../../types/fgts";
import { FgtsEventTable } from "./FgtsEventTable";

interface Props { form: FgtsProjectionForm; result: FgtsProjectionResult; events: FgtsAmortizationEvent[]; onFormChange: (form: FgtsProjectionForm) => void; onEventsChange: (events: FgtsAmortizationEvent[]) => void; }
const cls = "mt-2 h-12 w-full rounded-lg border border-slate-300 bg-white px-3 text-sm font-medium outline-none focus:border-goodgreen-500 focus:ring-4 focus:ring-goodgreen-500/10";

export function FgtsAmortizationPlanner({ form, result, events, onFormChange, onEventsChange }: Props) {
  const active = events.filter((e) => e.active && e.amount > 0);
  function addEvent() { const last = events[events.length - 1]; const month = (last?.month || 0) + form.periodicityMonths; onEventsChange([...events, { id: `manual-${Date.now()}`, eventNumber: events.length + 1, month, estimatedDate: "", availableBalance: 0, amount: 0, remainingBalance: 0, active: true, note: "Evento adicionado", source: "FGTS" }]); }
  return <section className="rounded-lg border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-xl font-bold">Planejamento de Amortizações com FGTS</h2><p className="mt-1 text-sm text-slate-500">Projete utilizações futuras e revise cada evento antes de enviar à planilha.</p></div><button type="button" className="btn-secondary" onClick={addEvent}><Plus className="h-4 w-4" /> Adicionar evento</button></div><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-4"><label className="text-sm font-semibold">Data do contrato (opcional)<input type="date" className={cls} value={form.contractDate} onChange={(e) => onFormChange({ ...form, contractDate: e.target.value })} /></label><label className="text-sm font-semibold">Última utilização<input type="date" className={cls} disabled={form.neverUsed} value={form.lastUsageDate} onChange={(e) => onFormChange({ ...form, lastUsageDate: e.target.value })} /></label><label className="flex items-center gap-3 self-end rounded-lg border border-slate-200 p-3 text-sm font-semibold"><input type="checkbox" checked={form.neverUsed} onChange={(e) => onFormChange({ ...form, neverUsed: e.target.checked, lastUsageDate: e.target.checked ? "" : form.lastUsageDate })} />Nunca utilizou nesta modalidade</label><label className="text-sm font-semibold">Prazo restante (meses)<input type="text" inputMode="numeric" className={cls} value={form.remainingTermMonths} onChange={(e) => onFormChange({ ...form, remainingTermMonths: Math.max(1, Number(e.target.value.replace(/\D/g, "")) || 1) })} /></label><label className="text-sm font-semibold">Saldo atual disponível<input type="text" inputMode="decimal" className={cls} value={form.currentBalance || ""} onChange={(e) => onFormChange({ ...form, currentBalance: parseNumberBR(e.target.value) })} /></label><label className="text-sm font-semibold">Depósito mensal médio<input type="text" inputMode="decimal" className={cls} value={form.monthlyDeposit || ""} onChange={(e) => onFormChange({ ...form, monthlyDeposit: parseNumberBR(e.target.value) })} /></label><label className="text-sm font-semibold">Primeiro aporte (mês)<input type="text" inputMode="numeric" className={cls} value={form.firstContributionMonth} onChange={(e) => onFormChange({ ...form, firstContributionMonth: Math.max(1, Number(e.target.value.replace(/\D/g, "")) || 1) })} /></label><label className="text-sm font-semibold">Periodicidade (meses)<input type="text" inputMode="numeric" className={cls} disabled={!form.advancedPeriodicity} value={form.periodicityMonths} onChange={(e) => onFormChange({ ...form, periodicityMonths: Math.max(1, Number(e.target.value.replace(/\D/g, "")) || 24) })} /></label><label className="flex items-center gap-3 rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900"><input type="checkbox" checked={form.advancedPeriodicity} onChange={(e) => onFormChange({ ...form, advancedPeriodicity: e.target.checked, periodicityMonths: e.target.checked ? form.periodicityMonths : 24 })} />Modo avançado de periodicidade</label><label className="text-sm font-semibold md:col-span-2">Forma de definição<select className={cls} value={form.contributionMode} onChange={(e) => onFormChange({ ...form, contributionMode: e.target.value as FgtsProjectionForm["contributionMode"] })}><option value="FIXED">Valor fixo informado manualmente</option><option value="CURRENT_BALANCE">Utilizar todo o saldo atual no primeiro aporte</option><option value="MONTHLY_PROJECTION">Projetar saldo com base nos depósitos mensais</option><option value="INDIVIDUAL">Informar valores individualmente por evento</option></select></label>{["FIXED", "INDIVIDUAL"].includes(form.contributionMode) && <label className="text-sm font-semibold">Valor por aporte<input type="text" inputMode="decimal" className={cls} value={form.fixedAmount || ""} onChange={(e) => onFormChange({ ...form, fixedAmount: parseNumberBR(e.target.value) })} /></label>}<label className="text-sm font-semibold">Último mês (opcional)<input type="text" inputMode="numeric" className={cls} value={form.lastMonth || ""} onChange={(e) => onFormChange({ ...form, lastMonth: Number(e.target.value.replace(/\D/g, "")) || undefined })} /></label></div>{result.nextEligibleDate && <p className="mt-4 rounded-lg border border-blue-200 bg-blue-50 p-4 text-sm text-blue-800"><strong>Próxima utilização indicativa:</strong> {new Date(`${result.nextEligibleDate}T12:00:00`).toLocaleDateString("pt-BR")}. O prazo é analisado por trabalhador e depende da confirmação do agente financeiro.</p>}<FgtsEventTable events={events} onChange={onEventsChange} /><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs text-slate-500">Utilizações</p><p className="font-bold">{active.length}</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs text-slate-500">Total projetado</p><p className="font-bold">{formatCurrencyBR(active.reduce((sum, e) => sum + e.amount, 0))}</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs text-slate-500">Primeiro aporte</p><p className="font-bold">Mês {active[0]?.month || "—"}</p></div><div className="rounded-lg bg-slate-50 p-4"><p className="text-xs text-slate-500">Último aporte</p><p className="font-bold">Mês {active.at(-1)?.month || "—"}</p></div></div><p className="mt-4 text-xs leading-5 text-slate-500">A projeção considera valores informados pelo usuário e não garante saldo futuro, liberação ou autorização de uso. Não inclui correção monetária, mudanças salariais, saques, bloqueios ou mudanças de vínculo.</p></section>;
}
